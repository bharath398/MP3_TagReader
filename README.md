# MP3_TagReader
An MP3 Tag Reader refers to a tool, library, or class that reads metadata (tags) embedded in MP3 audio files — things like the song title, artist, album, genre, year, track number, and album art. This metadata is typically stored using the ID3 tag standard
